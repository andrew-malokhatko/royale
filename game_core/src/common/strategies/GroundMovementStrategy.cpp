#pragma once

#include "movement/GroundMovementStrategy.hpp"
#include "Game.hpp"

namespace royale
{
	void GroundMovementStrategy::move(GameObject& gameObject)
	{
		const GameContext& context = gameObject.getContext();
	}
}